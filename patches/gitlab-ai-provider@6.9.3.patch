diff --git a/dist/index.js b/dist/index.js
index cb041a8eec3bc39fb00743580c0566d36e666594..651bd130da0ef44bdcac337804bca0885de26ef3 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -3314,7 +3314,7 @@ var crypto = __toESM(require("crypto"));
 var DISCOVERY_TTL_MS = 10 * 60 * 1e3;
 function getCacheFilePath() {
   const cacheHome = process.env.XDG_CACHE_HOME || path3.join(os.homedir(), ".cache");
-  return path3.join(cacheHome, "opencode", "gitlab-workflow-model-cache.json");
+  return path3.join(cacheHome, "slopcode", "gitlab-workflow-model-cache.json");
 }
 function computeCacheKey(workDir, instanceUrl) {
   const normalizedUrl = (instanceUrl || "https://gitlab.com").replace(/\/$/, "");
@@ -5035,20 +5035,27 @@ var GitLabOAuthManager = class {
 var fs3 = __toESM(require("fs"));
 var path4 = __toESM(require("path"));
 var os2 = __toESM(require("os"));
-function getOpenCodeAuthPath() {
+function getSlopcodeAuthPath(app = "slopcode") {
   const homeDir = os2.homedir();
   const xdgDataHome = process.env.XDG_DATA_HOME;
   if (xdgDataHome) {
-    return path4.join(xdgDataHome, "opencode", "auth.json");
+    return path4.join(xdgDataHome, app, "auth.json");
   }
   if (process.platform !== "win32") {
-    return path4.join(homeDir, ".local", "share", "opencode", "auth.json");
+    return path4.join(homeDir, ".local", "share", app, "auth.json");
   }
-  return path4.join(homeDir, ".opencode", "auth.json");
+  return path4.join(homeDir, `.${app}`, "auth.json");
 }
-async function loadOpenCodeAuth(instanceUrl) {
+function getLegacyAuthPath() {
+  return getSlopcodeAuthPath("opencode");
+}
+function getReadableAuthPath() {
+  const current = getSlopcodeAuthPath();
+  return fs3.existsSync(current) ? current : getLegacyAuthPath();
+}
+async function loadSlopcodeAuth(instanceUrl) {
   try {
-    const authPath = getOpenCodeAuthPath();
+    const authPath = getReadableAuthPath();
     if (!fs3.existsSync(authPath)) {
       return void 0;
     }
@@ -5083,8 +5090,8 @@ async function refreshOAuthToken(auth, instanceUrl, clientId) {
       refreshToken: auth.refresh,
       clientId
     });
-    const authPath = getOpenCodeAuthPath();
-    const authData = JSON.parse(fs3.readFileSync(authPath, "utf-8"));
+    const authPath = getSlopcodeAuthPath();
+    const authData = JSON.parse(fs3.readFileSync(getReadableAuthPath(), "utf-8"));
     authData.gitlab = {
       type: "oauth",
       refresh: refreshed.refreshToken,
@@ -5093,6 +5100,7 @@ async function refreshOAuthToken(auth, instanceUrl, clientId) {
       enterpriseUrl: instanceUrl
       // Use enterpriseUrl to match auth plugin format
     };
+    fs3.mkdirSync(path4.dirname(authPath), { recursive: true });
     fs3.writeFileSync(authPath, JSON.stringify(authData, null, 2), { mode: 384 });
     return refreshed.accessToken;
   })();
@@ -5107,7 +5115,7 @@ async function loadApiKey(options, instanceUrl, clientId) {
   if (options.apiKey) {
     return options.apiKey;
   }
-  const auth = await loadOpenCodeAuth(instanceUrl);
+  const auth = await loadSlopcodeAuth(instanceUrl);
   if (auth?.type === "api") {
     return auth.key;
   }
@@ -5123,7 +5131,7 @@ async function loadApiKey(options, instanceUrl, clientId) {
           return envApiKey;
         }
         throw new GitLabError({
-          message: `OAuth token refresh failed and no fallback ${options.environmentVariableName} environment variable is set. Refresh error: ${refreshErrorMsg}. Re-authenticate with 'opencode auth login gitlab' or set ${options.environmentVariableName}.`
+          message: `OAuth token refresh failed and no fallback ${options.environmentVariableName} environment variable is set. Refresh error: ${refreshErrorMsg}. Re-authenticate with 'slopcode auth login gitlab' or set ${options.environmentVariableName}.`
         });
       }
     } else {
@@ -5133,7 +5141,7 @@ async function loadApiKey(options, instanceUrl, clientId) {
   const apiKey = process.env[options.environmentVariableName];
   if (!apiKey) {
     throw new GitLabError({
-      message: `${options.description} API key is missing. Pass it as the 'apiKey' parameter, set the ${options.environmentVariableName} environment variable, or authenticate with 'opencode auth login gitlab'.`
+      message: `${options.description} API key is missing. Pass it as the 'apiKey' parameter, set the ${options.environmentVariableName} environment variable, or authenticate with 'slopcode auth login gitlab'.`
     });
   }
   return apiKey;
@@ -5202,7 +5210,7 @@ function createGitLab(options = {}) {
     const apiKey = cachedApiKey || options.apiKey || process.env["GITLAB_TOKEN"] || "";
     if (!apiKey) {
       throw new GitLabError({
-        message: "GitLab API key is missing. Pass it as the 'apiKey' parameter, set the GITLAB_TOKEN environment variable, or authenticate with 'opencode auth login gitlab'."
+        message: "GitLab API key is missing. Pass it as the 'apiKey' parameter, set the GITLAB_TOKEN environment variable, or authenticate with 'slopcode auth login gitlab'."
       });
     }
     return withUserAgentSuffix(
@@ -5334,7 +5342,7 @@ var DEFAULT_CONTEXT = 2e5;
 var DEFAULT_OUTPUT = 64e3;
 function getCacheFilePath2() {
   const cacheHome = process.env.XDG_CACHE_HOME || path5.join(os3.homedir(), ".cache");
-  return path5.join(cacheHome, "opencode", "gitlab-model-configs.json");
+  return path5.join(cacheHome, "slopcode", "gitlab-model-configs.json");
 }
 function readCacheFile() {
   try {
diff --git a/dist/index.mjs b/dist/index.mjs
index 95314906ea1693fb9828aa46eb7c7bc02444a7d8..73b02a2b71bedfee17711993da1440eb8761d94d 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -3235,7 +3235,7 @@ import * as crypto from "crypto";
 var DISCOVERY_TTL_MS = 10 * 60 * 1e3;
 function getCacheFilePath() {
   const cacheHome = process.env.XDG_CACHE_HOME || path3.join(os.homedir(), ".cache");
-  return path3.join(cacheHome, "opencode", "gitlab-workflow-model-cache.json");
+  return path3.join(cacheHome, "slopcode", "gitlab-workflow-model-cache.json");
 }
 function computeCacheKey(workDir, instanceUrl) {
   const normalizedUrl = (instanceUrl || "https://gitlab.com").replace(/\/$/, "");
@@ -4956,20 +4956,27 @@ var GitLabOAuthManager = class {
 import * as fs3 from "fs";
 import * as path4 from "path";
 import * as os2 from "os";
-function getOpenCodeAuthPath() {
+function getSlopcodeAuthPath(app = "slopcode") {
   const homeDir = os2.homedir();
   const xdgDataHome = process.env.XDG_DATA_HOME;
   if (xdgDataHome) {
-    return path4.join(xdgDataHome, "opencode", "auth.json");
+    return path4.join(xdgDataHome, app, "auth.json");
   }
   if (process.platform !== "win32") {
-    return path4.join(homeDir, ".local", "share", "opencode", "auth.json");
+    return path4.join(homeDir, ".local", "share", app, "auth.json");
   }
-  return path4.join(homeDir, ".opencode", "auth.json");
+  return path4.join(homeDir, `.${app}`, "auth.json");
 }
-async function loadOpenCodeAuth(instanceUrl) {
+function getLegacyAuthPath() {
+  return getSlopcodeAuthPath("opencode");
+}
+function getReadableAuthPath() {
+  const current = getSlopcodeAuthPath();
+  return fs3.existsSync(current) ? current : getLegacyAuthPath();
+}
+async function loadSlopcodeAuth(instanceUrl) {
   try {
-    const authPath = getOpenCodeAuthPath();
+    const authPath = getReadableAuthPath();
     if (!fs3.existsSync(authPath)) {
       return void 0;
     }
@@ -5004,8 +5011,8 @@ async function refreshOAuthToken(auth, instanceUrl, clientId) {
       refreshToken: auth.refresh,
       clientId
     });
-    const authPath = getOpenCodeAuthPath();
-    const authData = JSON.parse(fs3.readFileSync(authPath, "utf-8"));
+    const authPath = getSlopcodeAuthPath();
+    const authData = JSON.parse(fs3.readFileSync(getReadableAuthPath(), "utf-8"));
     authData.gitlab = {
       type: "oauth",
       refresh: refreshed.refreshToken,
@@ -5014,6 +5021,7 @@ async function refreshOAuthToken(auth, instanceUrl, clientId) {
       enterpriseUrl: instanceUrl
       // Use enterpriseUrl to match auth plugin format
     };
+    fs3.mkdirSync(path4.dirname(authPath), { recursive: true });
     fs3.writeFileSync(authPath, JSON.stringify(authData, null, 2), { mode: 384 });
     return refreshed.accessToken;
   })();
@@ -5028,7 +5036,7 @@ async function loadApiKey(options, instanceUrl, clientId) {
   if (options.apiKey) {
     return options.apiKey;
   }
-  const auth = await loadOpenCodeAuth(instanceUrl);
+  const auth = await loadSlopcodeAuth(instanceUrl);
   if (auth?.type === "api") {
     return auth.key;
   }
@@ -5044,7 +5052,7 @@ async function loadApiKey(options, instanceUrl, clientId) {
           return envApiKey;
         }
         throw new GitLabError({
-          message: `OAuth token refresh failed and no fallback ${options.environmentVariableName} environment variable is set. Refresh error: ${refreshErrorMsg}. Re-authenticate with 'opencode auth login gitlab' or set ${options.environmentVariableName}.`
+          message: `OAuth token refresh failed and no fallback ${options.environmentVariableName} environment variable is set. Refresh error: ${refreshErrorMsg}. Re-authenticate with 'slopcode auth login gitlab' or set ${options.environmentVariableName}.`
         });
       }
     } else {
@@ -5054,7 +5062,7 @@ async function loadApiKey(options, instanceUrl, clientId) {
   const apiKey = process.env[options.environmentVariableName];
   if (!apiKey) {
     throw new GitLabError({
-      message: `${options.description} API key is missing. Pass it as the 'apiKey' parameter, set the ${options.environmentVariableName} environment variable, or authenticate with 'opencode auth login gitlab'.`
+      message: `${options.description} API key is missing. Pass it as the 'apiKey' parameter, set the ${options.environmentVariableName} environment variable, or authenticate with 'slopcode auth login gitlab'.`
     });
   }
   return apiKey;
@@ -5123,7 +5131,7 @@ function createGitLab(options = {}) {
     const apiKey = cachedApiKey || options.apiKey || process.env["GITLAB_TOKEN"] || "";
     if (!apiKey) {
       throw new GitLabError({
-        message: "GitLab API key is missing. Pass it as the 'apiKey' parameter, set the GITLAB_TOKEN environment variable, or authenticate with 'opencode auth login gitlab'."
+        message: "GitLab API key is missing. Pass it as the 'apiKey' parameter, set the GITLAB_TOKEN environment variable, or authenticate with 'slopcode auth login gitlab'."
       });
     }
     return withUserAgentSuffix(
@@ -5255,7 +5263,7 @@ var DEFAULT_CONTEXT = 2e5;
 var DEFAULT_OUTPUT = 64e3;
 function getCacheFilePath2() {
   const cacheHome = process.env.XDG_CACHE_HOME || path5.join(os3.homedir(), ".cache");
-  return path5.join(cacheHome, "opencode", "gitlab-model-configs.json");
+  return path5.join(cacheHome, "slopcode", "gitlab-model-configs.json");
 }
 function readCacheFile() {
   try {
