# SlopCode v0.1.39

- Completes Android editor parity for open, diagnostics, dirty guard, save, and diff dismiss workflows.
- Moves Android TUI packaging onto a Cargo-managed Rust runtime foundation.
- Documents remaining Android parity work as Rust renderer/plugin protocol follow-up, not Bun FFI dependency.
